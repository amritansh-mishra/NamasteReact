const user = () => {
  return (
    <div className="user">
      <h1>Amrit</h1>
      <h2>React Developer</h2>
    </div>
  );
};

export default user;

// This component is a simple functional component that returns a div with a class name "user" and contains an h1 and h2 element.
// The h1 element displays the name "Amrit" and the h2 element displays the designation "React Developer".
// It is exported as the default export of the module, allowing it to be imported and used in other files.
